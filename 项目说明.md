# 基于串口的机械臂动作录制软件

## 背景描述
本赛季工程机器人的取矿等固定动作是在代码中使用二维数组来描述一个动作，在创建一个新动作时，通过拖动机械臂进行示教，在目标位置停住机械臂，记录当前各个轴的位置，依次记录该动作中所有关键点的位置，并保存为二维数组。

## 软件功能需求
1. 使用串口接收消息，目前的消息格式是6个float+一个4字节的帧尾(参考VOFA+的JustFloat引擎)
2. 支持实时刷新收到的正确格式的消息(6个浮点数)，支持按下按键去保存当前时刻的消息(6个float)
3. 支持将本次录制的所有消息存储到文本文件中，并支持命名，并支持从文本文件中读出消息，整理成C/C++支持的C语言二维数组格式并复制到剪切板

## 实现要求
1. 任何子菜单都可以回到主菜单
2. 无需先验知识的对读者友好的用户手册